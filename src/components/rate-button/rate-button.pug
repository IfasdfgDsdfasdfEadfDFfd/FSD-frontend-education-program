mixin rate-button(name, count)
  .rate-button
    - for (var x = 1; x <= 5; x++)
      - var value = x + " star" + (x > 1) ? "s" : "";
      input.rate-button__star(type='radio', name=name, value=value, checked=x == count)

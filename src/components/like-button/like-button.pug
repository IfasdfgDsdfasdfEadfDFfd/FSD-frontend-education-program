mixin like-button(name, liked=false, count='0')
  .like-button
    - var id = name + "-id";
    input.like-button__element(
      type='checkbox',
      id=id,
      name=name,
      value=name,
      checked=liked
    )
    label.like-button__label(for=id)= count
    .like-button__border

mixin radio-buttons-group(group_name, items)
  .radio-buttons-group
    each item in items
      - var id = group_name + item.value + "-id";
        .radio-button
          input.radio-button__element(
            type='radio',
            value=item.value,
            name=group_name,
            id=id,
            checked=item.checked
          )
          label.radio-button__label(for=id)= item.value
